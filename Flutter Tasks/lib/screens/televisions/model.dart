class Product
{
  String name, imageUrl;
  double price;
  int model,inches;

  Product({ required this.name, required this.imageUrl, required this.price,required this.model,required this.inches });
}